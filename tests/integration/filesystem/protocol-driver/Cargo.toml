[package]
name = "filesystem-test-driver"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
blake3 = "1"
serde_json = "1"
tmux-agent-protocol = { path = "../../../../crates/protocol" }
uuid = { version = "1", features = ["v4"] }

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2"
