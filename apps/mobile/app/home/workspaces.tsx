import { PlaceholderScreen } from "../../src/ui/components/PlaceholderScreen";

/** Workspaces tab — design.md §9.3.2. */
export default function WorkspacesScreen() {
  return <PlaceholderScreen title="Workspaces" route="/home/workspaces" />;
}
