import { PlaceholderScreen } from "../src/ui/components/PlaceholderScreen";

/** Your SSH key — design.md §9.9. */
export default function KeyScreen() {
  return <PlaceholderScreen title="Your SSH key" route="/key" />;
}
