import { PlaceholderScreen } from "../../src/ui/components/PlaceholderScreen";

/** Add / edit host — design.md §9.2 (`new` for add). */
export default function HostEditScreen() {
  return <PlaceholderScreen title="Add host" route="/hosts/[id]" />;
}
