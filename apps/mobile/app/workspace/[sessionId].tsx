import { PlaceholderScreen } from "../../src/ui/components/PlaceholderScreen";

/** Workspace — design.md §9.4. */
export default function WorkspaceScreen() {
  return <PlaceholderScreen title="Workspace" route="/workspace/[sessionId]" />;
}
