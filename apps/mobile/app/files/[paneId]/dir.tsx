import { PlaceholderScreen } from "../../../src/ui/components/PlaceholderScreen";

/** Files, subdirectory — design.md §9.6 (`dir?path=`). */
export default function FilesDirScreen() {
  return <PlaceholderScreen title="Files" route="/files/[paneId]/dir?path=" />;
}
