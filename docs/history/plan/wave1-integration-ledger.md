# Wave 1 integration ledger

Integration base: `36c85bd784bd0e9ea744334d519b6868bde269ad`
Branch: `improvement/integration`
Preserved ignored baseline: `tmp/phase14/before-reviewed-final-20260815-2211/`
Baseline content digest at integration: `5170941206874bf7156ac068a1aad513b5e102f631f27cf6e4084efb05a527b4`

The preserved artifact's historical `summary.json` incorrectly labels
workspace/tab coverage `PARTIAL` despite its shaped SSH/Docker lane failing.
It is immutable and was not rewritten; the authoritative correction is
`tests/performance/optimization/before-reviewed-final-supersession.md`, which supersedes that row
to `NOT_CAPTURED` and records which deterministic coverage remains supported.

## Package outcomes

| Order | Package | Reviewed tip | Integration merge | Outcome |
| --- | --- | --- | --- | --- |
| 1 | C3 protocol/policy | `ad1fc5bde0ebf8876d84056b9483507036dc1b27` | `cb7e84b` | Merged. Exhaustive operation policy remains the canonical dispatcher classification; frame-limit fix included. |
| 2 | C4 transfer admission | `35fd0032f9780358c024229ac18d9e87d3fce173` | `9201ea7` | Merged. Transactional queued admission, spawn rollback, and exclusive destination leases included. |
| 3 | C1 shell/recovery | `d80945265d9f3cc958853d81c932f8f44d3f72c3` | `c90174e` | Merged with its prerequisite `a772741`. Typed shell targets, scoped recovery controller, transient drag state, and navigation coordinator included. |
| 4 | C2 hooks | `55fe0f9c562af4220817b157d370e7e198b795ae` | `15f4727` | Merged. Retryable hook disposition/fallback persistence and retryable setup review lifecycle included. |
| 5 | P2 pane/tmux | `dcac71f3092cbd394f7ee0e6022b8f69be817dc5` | `6e64645` | Merged. Incremental pane accounting/LRUs, single authoritative action precheck/postcheck, dirty-epoch suppression, and topology action reductions included. |
| 6 | P5 SSH/Tauri | `be866ff0aacff1d08c82ca1ed568af154611b463` | `06da4f3` | Merged. Cancellable reconnect, byte-bounded input, async resize, per-socket control-master coordination, warm leases, and bridge payload ownership included. |
| 7 | P1 terminal frontend | `b1d16dea80df5d72b08f199302deea581e01c56f` | `e1ca492` | Merged with its five prerequisite commits. Dedicated epoch observers, head-index queues, explicit byte ownership, and encoded cache accounting included. |

All seven reviewed tips are ancestors of the integrated head.

The package branches retain their historical ancestry from the earlier
measurement-foundation commit `b082f66`; they were not independently rebuilt
on `36c85bd`. Integration followed the explicit handoff instruction to merge
the exact already-reviewed tips, in the exact dependency order above, onto the
clean hardened Stage 0 head. Every textual conflict was resolved semantically
against Stage 0 instrumentation and every package was then exercised and
reviewed in the integrated history. Rewriting the supplied tips would have
violated that handoff and destroyed their reviewed identities.
This explicit integration handoff is the authorized process exception to the
plan's generic branch-base and pre-merge eligibility sequence: it required the
exact supplied identities and dependency order, followed by integration-wide
hard-gate review. No historical branch-tip CLEAN verdict is inferred from that
exception.

The handoff retained the following branch-gate outcomes. Where a task or
personal identity was not retained, this ledger says so rather than inventing
one:

| Branch | Retained thermo-nuclear branch gate | Final verdict |
| --- | --- | --- |
| Stage 0 | `stage0_thermo_frontend` (Gauss), `stage0_thermo_runtime` (Newton), `stage0_thermo_harness` (Lorentz), through seven rounds | All CLEAN / APPROVE |
| C1 | `/root/impl_c1_shell/c1_thermo_review_a` (Dalton), `/root/impl_c1_shell/c1_thermo_review_b` (Dirac), repeated rounds | Both APPROVED |
| C2 | `/root/impl_c2_hooks/c2_frontend_review` (Dewey), `/root/impl_c2_hooks/c2_host_review` (Avicenna), and `/root/impl_c2_hooks/c2_final_review` (personal name unavailable) | Round 2 clean; Round 3 blocker fixed and re-review clean |
| C3 | `/root/impl_c3_protocol_policy/c3_thermo_review_1`, `_2`, `_3` (personal names unavailable) | All CLEAN / no blockers |
| C4 | Handoff reported three clean reviews without retained identities. Recovered exact-base audit `/root/merge_wave1/wave1_c4_recovered_gate` reviewed `b082f66..35fd003`. | Historical tip **BLOCK**: per-job polling monitors, duplicate active-owner state, quadratic destination probing, and provisional cancellation were present. Later integrated fixes are identified below; the original branch gate cannot be represented as independently verified. |
| P1 | Handoff reported branch-local A/B clean after six rounds without retained identities. Recovered exact-base audit `/root/merge_wave1/wave1_p1_recovered_gate` reviewed `b082f66..b1d16de`. | Historical tip **BLOCK**: zero-byte ordering, unbounded zero-byte recovery records, active-pane eviction, multicast ownership/exception loss, and probabilistic exact identity were present. Later integrated fixes are identified below; the original branch gate cannot be represented as independently verified. |
| P2 | Handoff reported three clean reviews without retained identities. Recovered exact-base audit `/root/merge_wave1/wave1_p2_recovered_gate` reviewed `b082f66..dcac71f`. | Historical tip **BLOCK**: non-authoritative input barrier, non-atomic create/select, a new >1,000-line production stream, and missing required timing/shaped evidence were present. Later integrated fixes are identified below; the original branch gate cannot be represented as independently verified. |
| P5 | `/root/impl_p5_tauri_ssh/thermo_review_1` (Laplace), `_2` (Hubble), `_3` (Fermat) | All APPROVE / no blockers |

## Planned-item disposition

Stage 0's numbered measurement items are recorded separately because the
immutable baseline contains both valid deterministic artifacts and a failed
mandatory shaped lane. A code/reviewer pass is not presented as replacement
evidence for a missing runtime capture.

| Stage 0 item | Disposition | Evidence |
| --- | --- | --- |
| 1 terminal frontend counters and byte parity | Captured. | Preserved `logs/terminal-frontend.log`; exact 64 B, 1 KiB, and 64 KiB cases. |
| 2 pane-resource scans/LRU/budgets/reveal parity | Captured. | Preserved `logs/pane-resource.log`; 32/256/1,024 panes. |
| 3 desktop/host idle requests/events/CPU/RSS | Deterministic request/event counts captured; idle CPU and RSS are `NOT_CAPTURED`. | Preserved deterministic artifacts do not contain process CPU/RSS sampling. |
| 4 Explorer 4,096-entry paint/watch/row HWM | List/watch/row HWM fixture captured; expand/change-to-paint timing is `NOT_CAPTURED`. | Preserved `logs/explorer-wide.log`; no renderer paint timestamp pair. |
| 5 Git watcher/status process sharing | Captured deterministic fixture. | Preserved `logs/git-consumers.log`. |
| 6 startup/React/font/terminal/Monaco/editor spans | Build output captured; packaged startup and platform spans are `NOT_CAPTURED`. | `logs/desktop-frontend-build.log` proves compilation only, not runtime startup spans. |
| 7 transfer admission saturation and terminal outcomes | Captured and later rerun after integration. | Preserved `logs/transfer-admission.log`; integrated 130-admission fixture below. |
| 8 Tauri bridge stalled-consumer ingress/send/lag/RSS | Ingress/send instrumentation is captured; stalled-WebView lag/RSS retention is `NOT_CAPTURED`. | No runtime WebView-stall/RSS artifact exists, so the conditional P5 window is unevaluated. |
| 9 packaged exact-target workflow spans | `NOT_CAPTURED` in the immutable baseline because the mandatory shaped run failed. | `tests/performance/optimization/before-reviewed-final-supersession.md`; no `PARTIAL` claim is inherited from `summary.json`. |
| Mandatory shaped 100 ms SSH/Docker baseline | `NOT_CAPTURED`; the exact artifact is preserved rather than rewritten. Later after-source shaped evidence is comparative runtime proof, not a retroactive Stage 0 baseline. | Supersession record and preserved failed lane. |

| Package item | Disposition | Integrated owner |
| --- | --- | --- |
| C1 pure discriminated command-target resolution | Merged; explicit targets suppress ambient fallbacks. | `c90174e` |
| C1 coherent durable-identity recovery state | Merged; later hardened for same-profile reset and per-profile identity retention. | `c90174e`, `4c72d01`, `32c38d8` |
| C1 transient/rAF sidebar drag geometry | Merged. | `c90174e` |
| C1 evidence-gated terminal memo boundary | Merged without a topology deep comparison. The final remediation stabilizes the shell-navigation controller, memoizes the terminal-only surface, and proves an unrelated root notice render does not revisit a live pane. | `c90174e`, `2e4b22c` |
| C2 retryable hook-review loading | Merged with captured targets retained on failure. | `15f4727` |
| C2 typed live/replay ingest dispositions | Merged; protocol-owned exhaustive disposition replaces duplicate strings. | `15f4727`, `3e3108c` |
| C2 malformed/duplicate/redaction/consent/config rules | Preserved and regression-tested. | `15f4727` |
| C3 advertised-frame size accounting | Merged with concatenated-frame regression. | `cb7e84b` |
| C3 exhaustive operation policy | Merged; dead family axis subsequently removed. | `cb7e84b`, `3e3108c` |
| C3 read-only `AgentHostNaming` mutation policy | Merged and tested. | `cb7e84b` |
| C3 generated-operation compatibility / no protobuf rewrite | Merged; schema shape preserved. | `cb7e84b` |
| C4 exact final-leaf RAII reservation | Merged; later hardened for owner release, semantic aliases, and one incremental namespace per destination directory. | `9201ea7`, `85725ad`, `32c38d8` |
| C4 transactional queued admission | Merged; cancellation publication now linearizes only after queued-event commit/rollback. The final bounded publisher retains at most two lightweight events, never a runnable job or reservation, and emits one terminal rollback when a timed-out queued send succeeds late. | `9201ea7`, `4c72d01`, `85725ad`, `32c38d8`, `d3832ec` |
| C4 worker-spawn rollback / exactly-once terminalization | Merged and panic/failure tested. | `9201ea7`, `3e3108c` |
| C4 two-active/128-queued/security/publication invariants | Preserved; pending publication is outside the runnable FIFO, cancellation has an absolute bound, and control-client replacement signals exact-scope invalidation without a polling monitor. Exact shaped transfer evidence is bound after every transfer-source review round. | `9201ea7`, `3e3108c`, `d37efb6`, `2cc75c7`, `4c72d01`, `85725ad`, `32c38d8`, `d3832ec` |
| P1 accepted-epoch-only observer | Merged; the final ownership hardening contains every synchronous application/epoch observer after decode and preserves the exact cumulative ACK boundary through failures. | `e1ca492`, `18461d1` |
| P1 head-index append queues | Merged; zero-byte records later became bounded ordered barriers. | `e1ca492`, `85725ad`, `32c38d8` |
| P1 single byte-ownership boundary | Merged; hub enforces one consumer before scheduler ownership transfer. | `e1ca492`, `3e3108c` |
| P1 encoded cache-length reuse | Merged. | `e1ca492` |
| P1 seed/resource/LRU/generation/overflow/drain parity | Preserved; consolidated pane state and active/dormant ownership close retention and exception paths. The final simplification makes the exclusively owned dormant backlog event the only retained exact resource identity, and reveal now orders recovery admission before any subsequently visible output without holding the resource store across credit waits. | `e1ca492`, `3e3108c`, `d37efb6`, `4c72d01`, `85725ad`, `2e4b22c`, `18461d1` |
| P2 incremental pane/journal/LRU accounting | Merged with zero full scans and zero linear retain operations in the fixture. | `6e64645` |
| P2 hidden seed/replay ownership moves | Merged with output parity. | `6e64645` |
| P2 batched discovery / lazy bootstrap identity | Merged. | `6e64645` |
| P2 established-profile action process/RTT reduction | Merged in code. The immutable Stage 0 shaped workspace/tab baseline is `NOT_CAPTURED`; the final shaped after-source action/request/process evidence is recorded below and is not mislabelled as that missing baseline. | `6e64645`, `06da4f3`, `4517782`, `e14a423` |
| P2 atomic create/select and selection acknowledgement reuse | Merged, then serialized through navigation and made one host select/reconcile/publish acknowledgement. The shaped fixture restores its primary control client after cleanup so every measured create has the production ownership sequence. | `d37efb6`, `2cc75c7`, `4c72d01`, `4517782`, `e14a423` |
| P2 input barrier / one precheck and postcheck / dirty suppression | Merged; barrier now waits for exact tmux protocol completion, preserves failure ownership, bounds abandoned exact-ID waits, and aborts every parser/membership discard. Final flood hardening moves input to one output-independent, cross-session `no-output` tmux sidecar per protocol connection; its one FIFO and exact-ID fence preserve global order without small-input forks or unknown-outcome replay. The final fence admission uses the same absolute two-second deadline as its completion wait, including a full sidecar queue. Attachment and sidecar startup are transactional across every fallible child/pipe/thread stage. | `6e64645`, `85725ad`, `32c38d8`, `2e4b22c`, `cf7bce9`, `d3832ec`, `18461d1` |
| P2 exact membership no-op / one-pass session grouping | Merged. | `6e64645` |
| P2 consistency discovery beyond redundant identity fork | Preserved. | `6e64645` |
| P5 move/encode bridge payloads without detection clones | Merged. | `06da4f3` |
| P5 async final-size-wins resize | Merged; the sixth remediation bounds ownership to one latest waiter. | `06da4f3`, `32c38d8` |
| P5 byte + 512-message input bounds | Merged for binary and legacy input with synchronous retryable refusal. | `06da4f3` |
| P5 output-independent persistent input path | Added after the Round 8 shaped-flood gate proved the shared output client could not meet interaction latency. One tmux 3.3a-compatible `no-output,ignore-size,no-detach-on-destroy` sidecar serves every attached session on the connection; deterministic fixtures cover cross-session FIFO, target-session destruction, all-sessions-gone recreation, unknown-outcome death/no replay, stop, and transactional cleanup/retry at every worker-spawn failure. | `cf7bce9`, `18461d1` |
| P5 cancellable reconnect backoff | Merged. | `06da4f3` |
| P5 per-socket asynchronous master coordination / no duplicate check | Merged and lifecycle-race hardened. | `06da4f3`, `3e3108c`, `d37efb6` |
| P5 warm zero-lease master / explicit shutdown / startup lease pass | Merged. | `06da4f3`, `d37efb6` |
| P5 pipelined hello+subscribe / cached versions / local connecting | Merged; incompatible response is drained and quarantined. | `06da4f3`, `2cc75c7` |
| P5 conditional acknowledged WebView byte window | The immutable Stage 0 stalled-WebView lag/RSS condition remains `NOT_CAPTURED`, so no retroactive baseline claim is made. Round 8 clean-source shaped diagnostics independently exposed seconds of already-admitted host/socket debt and authorized the bounded path: new/new peers negotiate a 2 MiB / 1,024-raw-record host window, native retains a 2 MiB steady-state / 4,096-frame window and admits one protocol-bounded oversized recovery frame only while empty, and cumulative epoch-scoped ACKs release exact byte+record charges only after synchronous JS admission. Legacy peers do not block. Final validation proves the exact cumulative prefix without mutating the debit queue, drains only after a match, and reconnects on same-epoch rejection. | `cf7bce9`, `d3832ec`, `18461d1`; exact after-source evidence below. |

## Conflict decisions

- C4 `files/scheduler.rs`: used C4's admission barrier and worker-ownership rewrite. Ported Stage 0's typed accepted/rejected counters and success/failure completion counters to every terminal path. No event, cancellation entry, or lease survives rejected admission.
- C1 `App.tsx`, `TerminalPane.tsx`, and `perf/probe*`: kept C1's navigation/recovery decomposition, but retained Stage 0's paint-ticket timing and exact token + connection + pane targeting. `useTmuxActionPerformer` now opens, targets from the authoritative acknowledgement, and abandons those Stage 0 tokens.
- P2 `requests/dispatcher.rs`: kept C3's exhaustive policy dispatcher and added only P2's topology dirty-epoch signal to the tmux-action context.
- P2 `tmux_actions.rs`: kept P2's single precheck/postcheck action flow and restored Stage 0's authoritative pane ID on paint-measured action acknowledgements.
- P2 `replay.rs`: kept P2's incremental counters and linked LRUs. Retained Stage 0's test-thread-local measurement observer, so production stores carry no optional instrumentation state. Both baseline and incremental Phase 14 fixtures remain compiled.
- P5 `connection.rs`, `bridge.rs`, and `transport.rs`: kept P5's stop signal, dispatcher queues, lease-passing bridge startup, and per-socket coordinator. Ported Stage 0's UUID-scoped bridge measurement ownership and typed attempt/success/failure counters without restoring the removed global master implementation.
- P5 `tests/performance/optimization/run-before.sh`: retained Stage 0's hardened local binary helpers; updated the exact-terminal-outcome fixture name and added P5's serialized SSH coordination fixture.
- P1 `terminal/api.ts`: retained Stage 0's cumulative bridge acknowledgement/finalization protocol and P1's owned terminal bytes plus decoder operation recorder; measuring the owned bytes does not add another payload copy.

## Verification ledger

- `cargo fmt --all -- --check`: pass after conflict resolution and final review fixes.
- `git diff --check`: pass.
- `pnpm --filter @tmux-agent-ide/desktop exec tsc -b --pretty false`: pass on the fifth-review remediation source.
- `pnpm test`: fifth-review source pass, 82 files and 702 tests.
- `pnpm build`: fifth-review source pass (1,416 modules; only the existing Vite chunk-size advisory).
- Focused terminal hub/navigation/visible-session/command Vitest: 97 passed after the active/dormant registry, serialized create, and host-selection acknowledgement fixes. A further 49 focused hub/epoch-ack/recovery tests passed after the fourth review. The Phase 14 terminal fixture remained byte- and callback-exact at 64 B, 1 KiB, and 64 KiB; its operation proxy was 16 versus baseline 65.
- `cargo test --workspace --all-targets -- --test-threads=1`: the post-remediation run passed desktop (166 passed, 3 opt-in fixtures ignored) and every remaining crate/target, then exposed one stale CreateWindow fixture after the production postcondition was strengthened to require the created window active. The fixture was corrected and the complete host suite reran clean: 287 passed, 1 opt-in fixture ignored.
- Final desktop rerun on the exact final production source, including the generalized platform `stat` conversions: 170 passed, 3 opt-in fixtures ignored.
- Fourth-review `cargo test --workspace --all-targets -- --test-threads=1`: all crates and targets passed; desktop 170 passed / 3 opt-in ignored, host 288 passed / 1 opt-in ignored, hook CLI 4 passed, protocol 3 unit + 17 round-trip passed, tmux-control 31 passed / 2 opt-in ignored, and local tmux integration 2 passed.
- Fifth-review `cargo test --workspace --all-targets -- --test-threads=1`: all crates and targets passed; desktop 171 passed / 3 opt-in ignored, host 290 passed / 1 opt-in ignored, hook CLI 4 passed, protocol 3 unit + 17 round-trip passed, tmux-control 31 passed / 2 opt-in ignored, local tmux integration 2 passed, and phase-zero core 5 passed. Focused scheduler coverage passed 19 tests / 1 opt-in ignored; focused terminal scheduler, hub, and navigation coverage passed 106 tests.
- Sixth-review exact-source gates: TypeScript check passed; desktop passed 82 files / 704 tests; production build transformed 1,419 modules with only the existing chunk-size advisory. The quiet full Rust workspace rerun passed desktop 172 / 3 opt-in ignored, host 292 / 1 opt-in ignored, hook CLI 4, protocol 3 unit + 17 round-trip, tmux-control 31 / 2 opt-in ignored, local tmux integration 2, and phase-zero core 5. Exact all-feature `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`, and range diff-check passed with no allowances. Focused post-clippy coverage passed resize/dispatch 7 and admission 5.
- Seventh-review exact-source frontend gates at `be7c9cc`: TypeScript check passed; desktop Vitest passed 84 files / 708 tests; production build transformed 1,419 modules with only the existing chunk-size advisory.
- Seventh-review exact-source Rust gates at `be7c9cc`: the isolated host run passed 297 / 1 opt-in ignored, then `cargo test --workspace --all-targets -- --test-threads=1` passed every crate and target (desktop 176, host 297 / 1 opt-in ignored, hook CLI 4, protocol 17 round-trip, tmux-control 32 / 3 opt-in ignored, local tmux integration 2, and phase-zero core 5). The immediately preceding workspace attempt compiled successfully but was environmentally invalid: 53 host tests failed only while creating temp files/repos with `EDQUOT`; after removing only inactive generated cache data, the exact reruns were green. Exact all-feature `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo fmt --all -- --check`, worktree diff-check, and Stage0-to-head range diff-check passed with no allowances.
- Pre-final flow-control source gates at `cf7bce94eeace89e3bdcd4fad83a50e7bf2519f3`: `cargo fmt --all -- --check`, `git diff --check`, `cargo check --workspace --all-targets`, and `cargo clippy --workspace --all-targets -- -D warnings` passed with no allowances. `cargo test --workspace --all-targets` passed every crate and target; desktop Rust passed 178 / 3 opt-in ignored, host passed 312 / 1 opt-in ignored, hook CLI 4, protocol round trips, tmux-control, local tmux integration, and phase-zero core all passed. The two live isolated-tmux sidecar lifecycle fixtures passed in the full parallel run. TypeScript passed; desktop Vitest passed 85 files / 713 tests; `pnpm build` transformed 1,420 modules with only the existing chunk-size advisory. Focused input/credit/API checks passed 12 host input tests and 30 frontend API tests. A subsequent exact desktop rerun exposed a real admission/cancellation race, so this commit's evidence is preserved but superseded below.
- Final admission-race source gates at `3ee84b5bf92456971ddea6c41df1dd4a3e6ffa15`: a cancellation latched during queued-event publication is explicitly non-dispatchable until admission commits or rolls back, and cancellation atomically removes the committed queue entry. Both publication outcomes have deterministic latch regressions; the successful-publication race passed 100/100 repeated process-level iterations. `cargo fmt --all -- --check`, range `git diff --check`, `cargo check --workspace --all-targets`, and `cargo clippy --workspace --all-targets -- -D warnings` passed with no allowances. `cargo test --workspace --all-targets` passed every crate and target (desktop 178 / 3 opt-in ignored, host 312 / 1 opt-in ignored, hook CLI 4, and every protocol/tmux/core target). TypeScript passed; desktop Vitest passed 85 files / 713 tests; production build transformed 1,420 modules with only the existing chunk-size advisory.
- Round 9 remediation source gates at `d3832eccd046bd4a984a4390c4a7fbba5beb5ff0`: `cargo fmt --all -- --check`, `git diff --check`, `cargo check --workspace --all-targets`, and `cargo clippy --workspace --all-targets -- -D warnings` passed without allowances. The serial full Rust workspace passed desktop 182 / 3 opt-in ignored, host 313 / 1 opt-in ignored, hook CLI 4, protocol 3 unit + 17 round-trip, tmux-control 32 / 3 opt-in ignored, local tmux integration 2, and phase-zero core 5. Focused scheduler coverage passed 22 / 1 opt-in ignored; native delivery passed 4 plus the exact oversized PaneResource crossing; the host input sidecar passed 6. TypeScript passed, desktop Vitest passed 85 files / 713 tests, and production build transformed 1,420 modules with only the existing chunk-size advisory.
- Round 10 remediation source gates at `03397ca7c22c1d5572dc4eb14b3482216c9764df`: `cargo fmt --all -- --check`, `git diff --check`, `cargo check --workspace --all-targets`, and `cargo clippy --workspace --all-targets -- -D warnings` passed without allowances. The serial full Rust workspace passed desktop 189 / 3 opt-in ignored, host 315 / 1 opt-in ignored, and every remaining crate and target. Focused coverage passed the empty/omitted snapshot regression (1), bounded publication and spawn-failure regressions (4), ordinary late-PID termination-latch regressions (2), and empty-topology CreateSession bootstrap regressions (2). TypeScript passed; focused terminal recovery/delivery coverage passed 79 tests; desktop Vitest passed 85 files / 714 tests; and the production build transformed 1,420 modules with only the existing chunk-size advisory.
- Round 11 remediation source gates at `18461d1d70fc96b81836ece1f89a1d603bc66ffa`: `cargo fmt --all -- --check`, range and cached diff checks, `cargo check -p tmux-ide-host -p tmux-agent-desktop --all-targets`, and `cargo clippy --workspace --all-targets -- -D warnings` passed without allowances. The corrected serial full Rust workspace rerun passed desktop 189 / 3 opt-in ignored, host 319 / 1 opt-in ignored, hook CLI 4, protocol 3 unit + 17 round-trip, tmux-control 33 / 3 opt-in ignored, local tmux integration 2, and phase-zero core 5. The first full run exposed that the newly disarmed startup guard still tripped the live client's failure flag; the guard was corrected, both affected persistent-input lifecycle tests passed individually, and the complete workspace rerun above passed. Focused Round 11 coverage passed cumulative ACK prefix preservation and invalid-ACK reconnect, partial-frame writer poisoning, generic JS observer ownership plus next cumulative boundary, blocked-credit reveal-before-output and failed-admission seed debt, and first/second attachment and input-sidecar worker-spawn kill/reap/retry. TypeScript passed; focused API/hub coverage passed 69 tests; desktop Vitest passed 85 files / 715 tests; and the production build transformed 1,420 modules with only the existing chunk-size advisory.
- Sixth-review transfer admission fixture: 130 accepted, active high water 2, queued high water 128, 1 promptly rejected, 2 successes + 128 failures, exact completion parity.
- Sixth-review terminal recovery lanes: healthy 8-second pause passed with one pause, no stall/resync/gap, visible and hidden panes advanced and matched ground truth; one injected rejected resume also recovered both panes with no stall/resync/gap. VT parity passed all five seed/hide-reveal/pause/recovery/reconnect quiesce points with zero text-row or attribute-cell mismatches. A non-envelope stress experiment that injected twelve consecutive resume rejections exhausted the bounded retry and failed the hidden-pane round; a first exact one-rejection attempt did not trigger tmux pause and therefore failed the fixture precondition, while the immediate one-rejection rerun above triggered and passed. Neither failed attempt is presented as product coverage.
- The first final host attempt reached 264 passes before tmpfs quota errors caused 23 temp-creation failures; after removing only verified generated caches, the fresh-target rerun above was fully green. Two production-build attempts likewise stopped at errno 122 after TypeScript or the 1,413-module transform; the clean retry completed after the same generated-cache cleanup.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`: pass with no command-line allowances. The new `destination_lease` test module now follows production items, and platform-width conversions use one generic checked boundary rather than target-specific casts.
- `env UV_CACHE_DIR=tmp/work/uv-cache uv run --no-project tests/performance/optimization/test-summarize-baseline.py`: pass; failed or absent deterministic/shaped artifacts now produce `NOT_CAPTURED` rather than unconditional workflow coverage claims.
- Phase 14 transfer admission: 130 accepted, high water 2 active / 128 queued, 1 rejected, 2 successes + 128 failures, exact completion parity.
- Phase 14 SSH coordination: same socket started one master with active high water 1; different sockets started two masters with active high water 2.
- Phase 14 pane resources: baseline/incremental reveal parity passed through 1,024 panes; zero full accounting scans and zero linear LRU retain operations; sparse eviction released the old pane and retained the new pane.
- Exact release-mode P2 timing comparison used the same Phase 14 per-operation fixture at Stage 0 `36c85bd` and final tmux source `be7c9cc`, with nine samples per size. Stage 0 medians were 256 ns/op at 32 panes and 3,548 ns/op at 1,024 panes (samples: 32 `[237,239,247,256,256,263,264,268,272]`; 1,024 `[3464,3474,3506,3527,3548,3565,3615,4004,4800]`). Integrated medians were 401 ns/op at 32 panes and 272 ns/op at 1,024 panes (samples: 32 `[387,389,394,400,401,401,407,407,442]`; 1,024 `[271,271,271,272,272,273,273,276,284]`). The integrated 1,024/32 ratio was 0.68x (budget <=2x), and integrated/Stage0 at 1,024 panes was 7.7% (budget <=50%). The Stage 0 archive received only the byte-identical final timing observer/ignored fixture needed to execute the comparison; no production source was changed.
- Local Phase 12 enforced performance lane: echo p95 0.109 ms, switch p95 0.045 ms, new-tab p95 7.131 ms, new-workspace p95 7.932 ms, resize p95 3.556 ms, output 38.999 MB/s, zero connection resyncs and sequence gaps. Docker was deliberately skipped for this local command, so shaped and renderer-only rows were reported as BLOCK/MISS rather than claimed.
- Seventh-review exact-source shaped Phase 12 evidence: `tmp/phase12-perf-20260816T105237Z-3406216/`, captured from `be7c9cc` with 100 ms Docker latency. The Docker lane passed echo 100.862 ms, switch 100.706 ms, new-tab 214.427 ms, new-workspace 214.864 ms, resize 104.262 ms, 11.039 MB/s sustained output, zero flood and connection-wide resyncs, and no sequence gap. Five window and five session rounds produced exactly five `CreateWindow` and five `CreateSession` action requests, zero stale-topology retries, and zero matching session restatements. The remote wrapper recorded 128 host tmux processes, including exactly five `new-window`, five `new-session`, 96 batched discoveries, seven control-client attachments, five `kill-window`, five `kill-session`, one split, two identity displays, and two other fixture/setup calls. This is auditable after-source process/request evidence; because the immutable Stage 0 shaped lane is `NOT_CAPTURED`, no unsupported before/after process reduction is claimed. Two preceding shaped attempts are retained only as failed diagnostics: the first exposed topology observation before the pushed snapshot, and the second at `tmp/phase12-perf-20260816T104738Z-3379140/` had correct create latency but one flood queue-overflow resync and four fixture-induced stale retries. The final driver and bounded backpressure fixes closed both failures before the passing capture.
- Superseded exact clean-source shaped Phase 12 evidence: `tmp/phase12-perf-20260816T145428Z-747214/`, captured from `cf7bce94eeace89e3bdcd4fad83a50e7bf2519f3`; `source-status.txt` is empty, source digest `8eaf2d4e0b466ea067641c132f018fffe4869c69755e07511ad0e690242a1374`, transfer-component digest `5d9bab54971c806c12c6bf169fa394765a2251f9b13fe2f436e53b1e490d7f21`. Both floods ran 60 seconds. Local sustained 39.794 MB/s with typing p95 0.763 ms and input-admission ACK p95 0.092 ms. The 100 ms Docker lane sustained 11.068 MB/s with ordinary echo p95 100.818 ms, typing p95 159.146 ms, and input-admission ACK p95 156.148 ms. Both lanes had zero flood resyncs, zero connection-wide resyncs, zero event-sequence gaps, and zero idle host frames. Docker create/switch/resize remained inside 500/150 ms budgets. The remote wrapper recorded 116 host tmux processes and eight attachments: seven output clients plus exactly one `no-output,ignore-size,no-detach-on-destroy` sidecar; small flood input created no per-input tmux process. Cleanup removed the disposable container.
- Superseded post-admission-fix Phase 12 evidence: `tmp/phase12-perf-20260816T151343Z-872091/`, captured from clean commit `d2dcc84561f103f61eb00acfbdd73edaea0ba537`; it remains immutable but predates the Round 9 delivery/publication remediation.
- Superseded Round 9 exact clean-source shaped Phase 12 evidence: `tmp/phase12-perf-20260816T161309Z-1215497/`, captured from clean ledger/source commit `b169a84935eff11d16412d55b59b6357c1c8ff48` (production remediation `d3832eccd046bd4a984a4390c4a7fbba5beb5ff0`); `source-status.txt` is empty, source digest `f790ef1824882e1515058b34e793f3376e05d65ad5597763b771cfb865b324b1`, and transfer-component digest `ebe3d090e899878ea38b6355a2806ce00922df5da40100c3fd81e0af6f989f5c`. Both floods ran 60 seconds. Local sustained 39.191 MB/s with typing p95 0.771 ms, input-admission ACK p95 0.091 ms, and echo p95 0.143 ms. The 100 ms Docker lane sustained 11.088 MB/s with typing p95 159.706 ms, input-admission ACK p95 156.820 ms, and echo p95 100.844 ms. Both lanes had zero flood resyncs, zero connection-wide resyncs, zero event-sequence gaps, and zero idle host frames. Docker switch/new-tab/new-workspace/resize were 100.660/212.205/212.041/104.155 ms. The wrapper recorded 116 host tmux processes and eight attachments: seven output clients plus exactly one no-output sidecar. Five pane reseeds in each lane were bounded informational recovery, not a resync or gap. Cleanup removed every disposable process and container.
- Superseded Round 10 exact clean-source shaped Phase 12 evidence: `tmp/phase12-perf-20260816T171351Z-1842200/`, captured from clean ledger/source commit `8cda334400980c1079f8515dbb97944e665be21a` (production remediation `03397ca7c22c1d5572dc4eb14b3482216c9764df`); `source-status.txt` is empty, source digest `93deef80fc2515c2b77b9ee6624718290581b6e9319f2f49c2c06a16c71770f1`, and transfer-component digest `0f44fc08ea043bf53c7acc25f320b7e19440b5ea9bf2df9a0888b251cbce4557`. Both floods ran 60 seconds. Local sustained 39.221 MB/s with typing p95 0.791 ms, input-admission ACK p95 0.091 ms, and echo p95 0.136 ms. The 100 ms Docker lane sustained 11.105 MB/s with typing p95 160.286 ms, input-admission ACK p95 156.694 ms, and echo p95 100.872 ms. Both lanes had zero flood resyncs, zero connection-wide resyncs, zero event-sequence gaps, and zero idle host frames. Docker switch/new-tab/new-workspace/resize were 100.671/212.051/211.673/104.160 ms. The wrapper recorded 116 host tmux processes and eight attachments: seven output clients plus exactly one no-output sidecar. Five pane reseeds in each lane were bounded informational recovery, not a resync or gap. Cleanup removed every disposable process and container.
- Final Round 11 exact clean-source shaped Phase 12 evidence: `tmp/phase12-perf-20260816T183453Z-2567910/`, captured from clean ledger/source commit `4d2120fcae69b9672dc97b5850b6b3cd8141e334` (production remediation `18461d1d70fc96b81836ece1f89a1d603bc66ffa`); `source-status.txt` is empty, its 906-entry source manifest has digest `770b470253c606c2961043b3d4b1a1f5b33cf88c04995853b3ca302121a3d375`, and its 55-entry transfer-component manifest has digest `0f44fc08ea043bf53c7acc25f320b7e19440b5ea9bf2df9a0888b251cbce4557`. Both floods ran exactly 60 seconds. Local sustained 39.243 MB/s with typing p95 0.769 ms across 991 samples, input-admission ACK p95 0.091 ms, and echo p95 0.122 ms. The 100 ms Docker lane sustained 11.093 MB/s with typing p95 160.222 ms across 300 samples, input-admission ACK p95 156.652 ms, and echo p95 100.804 ms. Both lanes had zero flood resyncs, zero connection-wide resyncs, zero event-sequence gaps, and zero idle host frames. Docker switch/new-tab/new-workspace/resize were 100.777/211.671/212.444/104.344 ms. The wrapper recorded 116 host tmux processes and eight attachments: seven output clients plus exactly one no-output sidecar. Five pane reseeds in each lane were bounded informational recovery, not a resync or gap. Cleanup removed every disposable process and container. The immutable Stage 0 shaped lane remains `NOT_CAPTURED`; these are absolute final-source budgets, not a fabricated before/after comparison.
- Flow-design diagnostics are retained as negative evidence, not final gates. Sidecar plus adjacent coalescing at `tmp/phase12-perf-20260816T140755Z-482510/` preserved Docker throughput (13.780 MB/s) but left typing p95 877.081 ms, proving host/socket byte debt remained. A 64-raw-record credit experiment at `tmp/phase12-perf-20260816T143226Z-607434/` reduced typing to 181.937 ms but throughput to 3.793 MB/s, proving the record window was below the shaped bandwidth-delay product. The 1,024-record candidate at `tmp/phase12-perf-20260816T143513Z-629680/` passed the short diagnostic and was then hardened, committed, and rerun for the exact 60-second artifact above. Rejected native-only/timer/pause/priority variants remain isolated in `wave1-rejected-native-ack-window-20260816` and `wave1-rejected-flow-control-experiments-20260816`; neither was applied to final source.
- Phase 12 idle steadiness: pass across 60 seconds idle plus 60 seconds controlled repainting; zero seeds, topology churn, snapshots, resyncs, or sequence gaps in either window.
- Phase 12 pause, VT-parity, and stall-recovery lanes: pass. All five VT quiesce points had zero text/attribute mismatch; the stalled connection survived and preserved topology.
- Phase 7 smoke first stopped safely at its tmpfs preflight because the integration worktree lives under `/tmp`; it performed no transfer. Re-run with only generated work/cache data on disk-backed `/home` passed the local 16 MiB transfer lane. The earlier shaped Docker/OpenSSH 64 MiB lane passed after the second-review fixes but is retained as historical evidence only because its manifest predates the final client-scope and third-review fixes.
- The `tmp/evidence/phase7-ssh.20260816T075804Z-2317538/` shaped run exactly bound production commit `2cc75c7` and passed its complete 64 MiB transfer/security contract. It became historical when the fourth-review source fixes below landed; fresh exact-source evidence is recorded after their remediation commit.
- Final fourth-review production evidence: `tmp/evidence/phase7-ssh.20260816T083323Z-2538709/`, captured from clean source commit `4c72d01894e908248e550fd4d4dbdf3296746aec`. Source digest `1221aa5a760e0aceab1cc497e581409f776b04bdad40c753712d5806be76ca1e`; transfer-component digest `799902d8e5744547c4b9c9a26dd57741822d66ffea9e515bedef66acf6617d5c`. Exact 64 MiB upload/download digests passed through the direct driver and production desktop manager; canonical active workers were exactly 2, SSH peaked at 3 total connections, manager control latency peaked at 51 ms across 74 probes, HWM was 13,256 KiB, and parent replacement failed closed with recovery and no attacker, backup, or partial publication. `sha256sum -c` matched every captured source file before this ledger-only evidence update.
- Fifth-review production evidence: `tmp/evidence/phase7-ssh.20260816T091519Z-2804883/`, captured from clean remediation commit `85725add27e843c0990151f84caa8ef46ef034d8`. Source digest `860f438d253e517441407c6b701ea9f5545dc8be1283c7aa91d095a3e1bd978c`; transfer-component digest `8918a1fe04aaa8490288a8ea9c4f3741711b1c4b9b7f4b97a3565d29f6afe656`. Both direct-driver and production-manager 64 MiB upload/download digests passed; canonical active workers were exactly 2, SSH peaked at exactly 3 total connections, direct control latency peaked at 102 ms across 42 probes, manager control latency peaked at 52 ms across 71 probes, and manager HWM was 13,172 KiB. Parent replacement failed closed with recovery and no attacker, backup, or partial publication. `sha256sum -c` matched all 882 captured source files before this ledger-only evidence update.
- Sixth-review production evidence: `tmp/evidence/phase7-ssh.20260816T100951Z-3143874/`, captured from clean remediation commit `32c38d8f1bc22541783adc64726184dea56cf033`. Source digest `1fa6c68f1b2e17e6dfa182dad35853c7820d1d5111ddb5e197fed752f6518376`; transfer-component digest `8d06defa6bd324d063ebfc25d9301e7ec9cd6f74586dc36eceefaeab40814a3e`. Both direct-driver and production-manager 64 MiB upload/download digests passed; canonical active workers were exactly 2, SSH peaked at exactly 3 total connections, direct control latency peaked at 102 ms across 41 probes, manager control latency peaked at 51 ms across 71 probes, and manager HWM was 13,056 KiB. Parent replacement failed closed with recovery and no attacker, backup, partial publication, or reconciliation manifest. `sha256sum -c` matched all 887 captured source files and all 52 transfer-component files before this ledger-only evidence update.
- Seventh-review final-source production evidence: `tmp/evidence/phase7-ssh.20260816T110653Z-3491206/`, captured from clean source/evidence commit `3c1c10e` (production tip `be7c9cc`). Source digest `ab723728323d9111c5cc4d701a9683ab238d4ccd248262a018cfb5d26c7c7126`; transfer-component digest `8d06defa6bd324d063ebfc25d9301e7ec9cd6f74586dc36eceefaeab40814a3e`. Both direct-driver and production-manager 64 MiB upload/download digests passed; canonical manager active workers were exactly 2, SSH peaked at exactly 3 total connections, direct control latency peaked at 101 ms across 45 probes, manager control latency peaked at 51 ms across 73 probes, and manager HWM was 13,108 KiB. Parent replacement failed closed with recovery and no attacker, backup, partial publication, or reconciliation manifest. The gate itself compared its before/after source manifests byte-for-byte; the captured manifest contains 893 source entries and 52 transfer-component entries. This bullet is the sole ledger-only change after the bound source capture.
- Superseded `cf7bce9` Phase 7 evidence: local `tmp/evidence/phase7-local.20260816T145832Z-775668/` and shaped Docker/OpenSSH `tmp/evidence/phase7-ssh.20260816T145956Z-787990/`. Both manifests have the same exact source and transfer digests as the superseded Phase 12 capture above. Direct-driver and production-manager BLAKE3 upload/download verification passed at 16 MiB local and 64 MiB shaped; the production manager retained exactly two active workers, queued cancellation completed, shaped SSH peaked at exactly three established connections, manager control latency peaked at 51 ms across 74 probes, and manager HWM was 13,072 KiB. The parent-swap security lane failed closed with no attacker, backup, partial, or reconciliation-manifest publication. All disposable containers and test processes were cleaned up.
- Superseded post-admission-fix Phase 7 evidence: local `tmp/evidence/phase7-local.20260816T151725Z-895602/` and shaped Docker/OpenSSH `tmp/evidence/phase7-ssh.20260816T151759Z-904217/`; both remain immutable but predate the Round 9 delivery/publication remediation.
- Superseded Round 9 exact 5 GiB Phase 7 evidence: local `tmp/evidence/phase7-local.20260816T161913Z-1253501/` and shaped Docker/OpenSSH `tmp/evidence/phase7-ssh.20260816T162218Z-1276337/`, captured from the same clean `b169a84935eff11d16412d55b59b6357c1c8ff48` content as the Round 9 Phase 12 gate. Both source manifests and both transfer-component manifests have exact digests `f790ef1824882e1515058b34e793f3376e05d65ad5597763b771cfb865b324b1` and `ebe3d090e899878ea38b6355a2806ce00922df5da40100c3fd81e0af6f989f5c`. Direct-driver and production-manager upload/download each transferred exactly 5,368,709,120 bytes with BLAKE3 verification. Both managers retained exactly two active workers and completed queued cancellation plus upload/download lifecycles. Local direct/manager control latency peaked at 9/10 ms with manager HWM 14,012 KiB. Shaped SSH peaked at exactly three established connections; direct/manager control latency peaked at 103/95 ms across 3,128/5,130 probes, and manager HWM was 33,672 KiB. Chunks never exceeded 1 MiB. Parent replacement failed closed with recovery and no attacker, backup, partial, or reconciliation-manifest publication. Source-tree before/after checks passed and every disposable container/process was removed.
- Superseded Round 10 exact 5 GiB Phase 7 evidence: local `tmp/evidence/phase7-local.20260816T171823Z-1873327/` and shaped Docker/OpenSSH `tmp/evidence/phase7-ssh.20260816T172036Z-1892573/`, captured from the same clean `8cda334400980c1079f8515dbb97944e665be21a` content as the superseded Round 10 Phase 12 gate. All three source manifests are byte-identical with 903 entries and digest `93deef80fc2515c2b77b9ee6624718290581b6e9319f2f49c2c06a16c71770f1`; all three transfer-component manifests are byte-identical with 55 entries and digest `0f44fc08ea043bf53c7acc25f320b7e19440b5ea9bf2df9a0888b251cbce4557`. Every manifest entry independently matched the captured source before its ledger-only edit. Direct-driver and production-manager upload/download each transferred exactly 5,368,709,120 bytes with BLAKE3 verification. Both managers retained exactly two active workers and completed queued cancellation plus upload/download lifecycles. Local direct/manager control latency peaked at 9/14 ms across 201/442 probes with manager HWM 14,396 KiB. Shaped SSH peaked at exactly three established connections; direct/manager control latency peaked at 103/102 ms across 3,093/5,130 probes, and manager HWM was 33,840 KiB. Chunks never exceeded 1 MiB. Parent replacement failed closed with recovery and no attacker, backup, partial, or reconciliation-manifest publication. Source-tree before/after checks passed and every disposable container/process was removed.
- Final Round 11 exact 5 GiB Phase 7 evidence: local `tmp/evidence/phase7-local.20260816T183958Z-2602080/` and shaped Docker/OpenSSH `tmp/evidence/phase7-ssh.20260816T184306Z-2625163/`, captured from the same clean `4d2120fcae69b9672dc97b5850b6b3cd8141e334` content as the final Round 11 Phase 12 gate. All three source manifests are byte-identical with 906 entries and digest `770b470253c606c2961043b3d4b1a1f5b33cf88c04995853b3ca302121a3d375`; all three transfer-component manifests are byte-identical with 55 entries and digest `0f44fc08ea043bf53c7acc25f320b7e19440b5ea9bf2df9a0888b251cbce4557`. `sha256sum -c` independently matched every one of the 906 captured source entries before this ledger-only edit. Direct-driver and production-manager upload/download in both lanes each transferred exactly 5,368,709,120 bytes and matched BLAKE3 `bcf27a182cee2a75728e2617d0ac5d90f902207f5332cf7190b345d96e9fd221`; chunks never exceeded 1 MiB. Both managers retained exactly two active workers and completed queued cancellation plus upload/download lifecycles. Local direct/manager control latency peaked at 8/11 ms across 200/441 probes with direct/manager HWM 12,436/14,032 KiB. Shaped SSH peaked at exactly three established connections; direct/manager control latency peaked at 103/100 ms across 3,139/5,131 probes with direct/manager HWM 12,552/33,520 KiB. Parent replacement failed closed with recovery and no attacker, backup, partial, or reconciliation-manifest publication. Source-tree before/after checks passed and every disposable container/process was removed.

## Deferred or out-of-scope

- P3 filesystem/Explorer, P4 Git, and P6 editor/startup are Wave 2+ and were not started.
- The mandatory Stage 0 shaped 100 ms before lane failed and remains `NOT_CAPTURED`; it cannot be reconstructed or relabelled after integration. This is an explicit evidence limitation of the authorized exact-tip handoff. Final source-matched absolute budgets and deterministic Stage 0 fixtures are reported on their own terms and are never presented as a fabricated shaped before/after comparison.
- The original Stage 0 stalled-WebView lag/RSS condition for P5 remains `NOT_CAPTURED`; the final native and host delivery bounds are implementation invariants proven by deterministic credit/close/reconnect tests and shaped active-consumer evidence, not a retroactive claim about that missing baseline artifact.
- macOS packaged-app/WKWebView proof and final `cua` QA belong to the later integrated/final QA stage; Linux-only checks cannot claim those platform rows. Linux 100 ms shaped workspace/tab after-metrics are captured above, while their missing Stage 0 shaped baseline remains explicitly `NOT_CAPTURED`.

## Thermo-nuclear integrated review

All reviewers read the complete exact `thermo-nuclear-code-quality-review/SKILL.md` before inspecting the integrated history and worktree. Initial verdicts were blockers:

| Reviewer | Scope | Initial blockers | Resolution |
| --- | --- | --- | --- |
| `wave1_ts_review` | TS/React terminal and C1/Stage 0 | Parallel per-pane ledgers in `TerminalEventHub`; owned mutable bytes crossing a multicast listener boundary. | Replaced the ledgers with one bounded `PaneStreamState` registry and atomic pane cleanup/eviction; enforced exactly one pane consumer and transfer-on-replay. |
| `wave1_rust_review` | Rust scheduler, SSH, Stage 0 | Bulk setup/handshake not cancellation-owned early enough; redundant scheduler admission owners and per-job polling monitors; first measured bridge frame preceding channel quiescence ownership. | Bound the spawned helper PID before cancellable nonblocking handshake I/O; made `TransferEngine` the sole admission owner with one monitor for at-most-two active jobs and authoritative queued validation at dequeue; made `TerminalEventChannel` clones share one RAII measurement owner created before the first frame. |
| `wave1_cross_review` | Cross-stack structure/history | Probabilistic resource fingerprint; parallel hub state; boolean control-master lifecycle; duplicated hook disposition strings; dead operation-policy axis; newly giant Rust files; incomplete ledger. | Stored bounded exact resource identities with conservative oversized reseed; applied the hub fix above; introduced typed `MasterLifecycle` and `LeaseRoute`; added protocol `HookIngestDisposition`; removed `OperationFamily`; extracted connection/service/stream tests and stream/control-socket helpers; completed this ledger. |

Additional integration verification exposed a shaped-SSH peak of four TCP connections. The bulk bridge pool already retains its direct SSH children, so the P5 bulk control master was a redundant second ownership mechanism. It was removed: the one interactive control master remains, while the bounded pool owns at most two independent direct bulk connections. The same 64 MiB shaped gate then passed at the expected peak of three connections. Control-master establishment also now requires a successful cancellable `ssh -O check` after socket publication before it is advertised ready.

The first fresh post-fix round also returned blockers; none was waived:

| Reviewer | Blockers | Resolution |
| --- | --- | --- |
| `wave1_final_rust_review` | Idle-expiry/shutdown could strand `Closing { in_flight: true }`; cancelled verifying uploads could not establish the production reconciliation lease; pool invalidation was server-wide and absent on reconnect; ledger diff-check claim was false. | Added one in-flight completion transition and deterministic shutdown race; introduced a deadline-bounded authoritative acquisition mode exercised through `BulkLease`; keyed invalidation by exact control-client token on reconnect and stop; removed Markdown trailing whitespace and reran range diff checks. |
| `wave1_final_ts_review` | Active pane state was evictable; create/select still used two tmux actions; ClientHello and Subscribe were not pipelined. | Pinned state with a live consumer while keeping dormant state bounded and conservatively indebted; made host create select within its one request and reduced frontend completion to local acknowledgement; wrote both handshake requests before the first read with an explicit ordering test. |
| `wave1_final_cross_review` | Confirmed the active-pane and create/select findings; revalidation could be lost during an in-flight probe; input flush reported success after a failed write; ledger evidence was stale. | Added monotonic revalidation generations and a deterministic concurrent-probe test; retained the first input dispatch error until the next barrier; applied the shared fixes above and corrected this ledger. |

The third fresh round also returned blockers; none was waived:

| Reviewer | Blockers | Resolution |
| --- | --- | --- |
| `wave1_round3_rust_review` | An authoritative upload deadline could fire before its reconciliation helper PID was bound, leaving a later silent helper unbounded. | Added an independent persistent transport-termination latch, atomic late-PID kill/rejection, authoritative spawn/handshake cancellation, and deterministic direct-binding plus production `BulkLease` pre-spawn deadline tests. |
| `wave1_round3_ts_review` | Create was outside the navigation coordinator; host session selection was redundantly restated; incompatible pipelined Subscribe was not drained/quarantined; active-pane pressure made hub delivery scan all mounted panes. | Made create operations unique serialized coordinator intents; moved selection, geometry reconciliation, acknowledgement, and topology onto one host action contract and suppressed matching frontend restatement; always drained request 2 and quarantined incompatible events; split active ownership from the bounded dormant LRU for O(1) mounted-pane delivery. |
| `wave1_round3_cross_review` | Create success topology preceded required client selection; accepted stale/disconnected/read-only input could be silently dropped before a successful barrier; exact clippy was red; shaped evidence did not bind the then-current client-scope source. | Selected and re-discovered before publishing/acknowledging; made every undeliverable accepted input persist a barrier error; made the exact all-feature `-D warnings` gate green without command-line allowances; captured fresh shaped evidence against committed production source `2cc75c7`. |

The fourth stable-tree review again returned blockers; none was waived:

| Reviewer | Blockers | Resolution |
| --- | --- | --- |
| `wave1_stable_rust_review` | Host tmux write failures were sent to abandoned completion receivers, so later barriers succeeded; desktop `connection/bridge.rs` crossed 1,000 lines. | Retained the first host dispatcher failure through exactly one authoritative barrier with deterministic failure/clean-barrier coverage; removed dead per-request completion channels; extracted bridge tests to a sibling module, reducing production `bridge.rs` to 843 lines. |
| `wave1_stable_ts_review` | Visible-session acknowledgement lacked terminal epoch; exact pane-resource identity copies escaped byte accounting/bounds; A -> reset -> B recovery forgot A. | Scoped acknowledgements/asserted facts to the native terminal epoch and covered same-client reconnect; charged dormant exact identity allocations to the per-pane/global budgets and HWM, releasing them on active ownership transfer even through listener exceptions; retained the last non-empty same-profile identity through reconnect reset with hook coverage. |
| `wave1_stable_cross_review` | Create-session selected before its new control client existed; provisional admission could acknowledge cancellation before queued publication failed; Stage 0 coverage labels were unconditional; confirmed the host barrier and giant-file findings. | Attached the authoritative post-create snapshot before selection while withholding ordered topology publication until selection/refresh; exposed cancellation only at atomic admission commit and covered publication-failure concurrency; derived coverage claims from passing lanes and required artifacts with a `uv` regression; applied the shared host/bridge fixes above. |

The fifth fresh review round also returned blockers; none was waived:

| Reviewer | Blockers | Resolution |
| --- | --- | --- |
| `wave1_round5_rust_review` | The process-lifetime transfer binding monitor polled an empty registry every 50 ms; cancellation was not addressable during queued-event publication. | Parked the monitor on a condition variable while the active-binding registry is empty and wake it on first insertion; atomically registered a provisional cancellation latch with the undispatchable queue slot, rolling both back on publication failure and covering success/failure publication races. |
| `wave1_round5_ts_review` | Zero-byte scheduler callbacks bypassed ordered terminal writes; backlog/live listener exceptions lost ownership without seed debt; navigation acknowledgement captured a stale callback; P5 evidence described a sequential rather than pipelined handshake. | Represented empty records as ordered queue barriers included in drain semantics; rolled back listener ownership and required an authoritative seed on every delivery exception; corrected the callback dependency and regression; corrected the evidence description to the implemented pipelined request/drain/quarantine contract. |
| `wave1_round5_cross_review` | Host barriers stopped at local write/flush rather than tmux protocol completion; confirmed hub delivery exceptions; the preserved Stage 0 summary's workspace coverage was internally false; `App.tsx` and `agents/mod.rs` remained giant orchestration/test files. | Correlated each serialized in-band input with tmux `%end`/`%error` before the authoritative barrier can complete; applied the hub fix above; preserved the exact immutable artifact and added an explicit supersession classifying the failed shaped row `NOT_CAPTURED`; extracted agent, shell-chrome, and host-settings controllers until `App.tsx` was 997 lines and moved the 1,229-line agent test module out, leaving production `agents/mod.rs` at 416 lines. |

The sixth fresh review round also returned blockers; none was waived:

| Reviewer | Blockers | Resolution |
| --- | --- | --- |
| `wave1_round6_rust_review` | Provisional cancellation was observable before queued publication committed or rolled back; resize retained an unbounded waiter vector and one blocking task per event; destination reservations rematerialized every held leaf and became quadratic; extracted agent tests crossed 1,000 lines. | Latched cancellation but withheld the result on a condition-variable admission linearization boundary; retained only the latest resize waiter and resolved superseded generations immediately; retained one incremental semantic probe namespace per exact destination directory; split agent tests into fallback/lifecycle/core files of 303/334/590 lines. |
| `wave1_round6_ts_review` | Zero-byte terminal records bypassed byte limits and could grow unbounded; recovery replacement identity was global rather than per host profile. | Added a separate in-flight record bound covering empty ordered barriers and surfaced byte/record counts on overflow; retained the last non-empty identity independently per profile and covered A-profile replacement across an intervening B-profile connection. |
| `wave1_round6_cross_review` | Anonymous host input completions and timeout abandonment could acknowledge the wrong request or consume a failure; `App.tsx` remained a threshold-sized heterogeneous coordinator; confirmed giant agent tests; supplied package tips had historical pre-hardened-Stage0 ancestry; package reviewer/item mapping was incomplete. | Added monotonic input IDs to marker, command block, completion, and exact-match barrier ownership with abandoned-barrier regression; extracted file actions, notice layer, and right rail, reducing `App.tsx` to 900 lines; applied the agent split above; documented the explicit exact-tip integration instruction and ancestry without rewriting supplied reviewed identities; added the branch-gate and exhaustive planned-item tables above. |

The seventh fresh review round also returned blockers; none was waived:

| Reviewer | Blockers | Resolution |
| --- | --- | --- |
| `wave1_round7_ts_review` (Maxwell) | Native save-panel completion could use a replaced host/root; dormant pane resources were copied into a second hidden identity ledger; navigation/controller props defeated the conditional terminal memo boundary; Stage 0/package evidence presentation was incomplete. | Captured exact scope/root across the picker and transfer publication with deferred regressions; made the exclusively owned backlog event the sole dormant resource identity; stabilized the navigation controller, memoized the terminal surface, and proved an unrelated root update does not revisit a live pane; enumerated every Stage 0 item and labelled missing shaped/package identities truthfully. |
| `wave1_round7_cross_review` (Darwin) | A timed-out desktop barrier could consume an accepted-input failure into an abandoned waiter; P2 shaped claims and placeholder commit ownership were not auditable. | Retained the desktop error until a live waiter accepts it and added a dropped-first/live-second regression; removed the false baseline/workspace claim, replaced remediation placeholders with exact commits, and captured the after-source action request and shaped latency evidence below. |
| `wave1_round7_rust_review` (Rawls) | Host input correlation could be abandoned on overlapping begin, membership removal, parser recovery, or EOF; the dispatcher wait was unbounded; the required 32/1,024-pane timing ratios were absent and LRU touches allocated owned strings. | Centralized exact-ID abort on every discard/EOF, bounded completion waiting, and covered timeout/stale/later progress; moved LRU links to stable `Arc<str>` keys, extracted recovery/LRU modules so production files remain below 1,000 lines, and captured the exact release timing comparison below. |

The eighth fresh round reviewed exact commit `f848f4ad532170a9f10cf9575316be8b0aed72cd` and also returned blockers; none was waived:

| Reviewer | Blockers | Resolution |
| --- | --- | --- |
| `/root/merge_wave1/wave1_round8_ts_review` (Galileo) | Lossless host backpressure made shaped flood typing p95 2.852 seconds; the mounted-pane recovery queue remained unbounded for zero-byte records; the shaped artifact was not source-bound and ran only 10 seconds. | Added the byte+record-bounded `DeferredTerminalOutputQueue`; made Phase 12 capture clean source status/manifests/digests and require 60 seconds when enforced; then replaced the shared-output input path and unbounded admitted-byte debt with the final sidecar plus cumulative-credit design in `cf7bce9`. |
| `/root/merge_wave1/wave1_round8_rust_review` (Bernoulli) | Confirmed flood starvation; desktop stdin mutex writes could strand teardown/timeouts; ambiguous hook acknowledgement retried another daemon; fallback mailbox pruning was not interprocess atomic; mandatory 60-second evidence was missing. | Added a cancellation-owned desktop writer actor whose deadline covers enqueue/write and whose teardown does not acquire the writer-held lock; made post-delivery hook ambiguity stay with one daemon; serialized mailbox prune/publish/consume across processes; applied final flow control and captured the exact 60-second evidence above. |
| `/root/merge_wave1/wave1_round8_cross_review` (Bohr) | Tmux actions performed redundant post-discovery; evidence disposition overstated missing Stage 0 rows; confirmed shaped flood starvation and the 10-second evidence gap; C4/P1/P2 historical branch reviews were not auditable. | Collapsed the action path to one authoritative post-discovery; classified unsupported Stage 0 rows `NOT_CAPTURED`; ran the exact final 60-second gate; recovered exact-base C4/P1/P2 audits under the retained task IDs above and recorded their BLOCK verdicts plus later integration-only fixes rather than inventing branch approval. |

The ninth fresh round reviewed exact evidence head `76beed46297fcd0170b7cc79c23d9080e70cf6f4`; all findings were blockers and none was waived:

| Reviewer | Blockers | Resolution |
| --- | --- | --- |
| `/root/merge_wave1/wave1_round9_ts_review` (Mendel) | A valid shared PaneResource larger than the 2 MiB steady-state window was rejected by native delivery although the host/protocol allowed it. | Admit one protocol-bounded oversized native frame only while the delivery window is empty, retain exact cumulative host charge, block every follower until its epoch-scoped ACK, and cover the complete encoded event crossing. |
| `/root/merge_wave1/wave1_round9_rust_review` (Ohm) | Confirmed oversized recovery rejection; `fence()` could block on queue admission before its deadline; arbitrary queued-event callbacks could block forever, occupy provisional FIFO ownership, and make cancellation unbounded; the active binding monitor polled every 50 ms inside a giant scheduler. | Applied the oversized-frame rule; made fence admission and completion share one absolute deadline; moved pending publication outside the runnable FIFO and replaced arbitrary production callbacks with a typed two-worker rendezvous publisher. A wedged send retains only one lightweight event, cancellation and resource release are bounded, a late success emits one terminal rollback, and a third wedged admission fails fast. Reconnect/stop now signal exact bulk-scope invalidation, the poller is deleted, and admission/lifecycle/publication are cohesive modules with `scheduler.rs` reduced to 671 lines. |
| `/root/merge_wave1/wave1_round9_cross_review` (Plato) | Confirmed the shared oversized recovery failure and challenged the supplied-tip ancestry/package-gate presentation. | Applied the shared delivery fix and recorded the explicit exact-tip handoff as the authorized process exception above, without rewriting historical commits or claiming recovered historical BLOCK gates were clean. |

The tenth fresh round reviewed exact evidence head `d5fc942700cb627ad24710f96bf596785edb3265`; all findings were blockers and none was waived:

| Reviewer | Blockers | Resolution |
| --- | --- | --- |
| `/root/merge_wave1/wave1_round10_ts_review` (Mendel) | Empty and omitted hidden snapshots collapsed to the same not-ready state, and a pane-listener exception created seed debt without advancing the cumulative JS delivery boundary. | Empty/omitted handoffs now release the hidden checkpoint and require an authoritative seed before later output; an atomically contained listener failure remains seed debt but consumes its wire frame so the next cumulative ACK boundary stays exact. Focused blank/omitted reveal and listener-failure/next-frame tests cover both transitions. |
| `/root/merge_wave1/wave1_round10_rust_review` (Ohm) | The input fence rejected fresh-server `CreateSession` when no sidecar could exist, and a buffered publication completion channel could time out after accepting a queued event without arranging its rollback. | Only the exact empty topology treats the absent sidecar as an already-complete fence; attachments without a sidecar remain fail-closed. Publication completion is now a zero-capacity rendezvous, so a success after admission timeout necessarily observes receiver closure and emits exactly one terminal rollback. |
| `/root/merge_wave1/wave1_round10_cross_review` (Plato) | Ordinary late helper PID binding did not honor the persistent stale/timeout termination latch, and publication worker spawn used `expect`, allowing a panic and partial actor pool. | Ordinary binding publishes the exact PID then applies the persistent latch and kills late stale/timeout children. Actor startup is fallible, cleans up a partially started worker, returns a retryable admission error, and has injected first/second-worker failure regressions proving no panic or residual pool. |

The eleventh fresh round reviewed exact evidence head `5d06c1e40c6be7d1381daae0392b982de38deae2`; every blocker was fixed in `18461d1d70fc96b81836ece1f89a1d603bc66ffa` and none was waived:

| Reviewer | Blockers | Resolution |
| --- | --- | --- |
| `/root/merge_wave1/wave1_round11_ts_review` (Euclid) | A generic synchronous application/epoch observer failure after decoded-frame ownership could still create a cumulative JS ACK ordinal hole; native ACK validation popped debits before proving the exact cumulative byte boundary and did not force reconnect on rejection. | Made decode the explicit ownership boundary and always records its exact cumulative frame/byte debit; contained app, epoch, and seed-request observers with a deterministic reconnect diagnostic; validates the entire native prefix without mutation, drains only after an exact boundary, and closes the epoch on invalid ACK. Throw-then-next-frame and invalid-then-correct-prefix regressions prove both ledgers. |
| `/root/merge_wave1/wave1_round11_rust_review` (Volta) | Reveal marked a pane Visible before its recovery event acquired credit/ordered admission, allowing concurrent higher-generation output to overtake it; a partial physical writer failure left the lane open for another protobuf frame; persistent attachment/input-client startup leaked process/thread ownership on fallible worker spawns. | Added one terminal emission-order fence spanning resource transition and ordered admission without holding `PaneResourceStore` across waits; failed credit or sequencer admission converts the speculative handoff to Released seed debt. Any physical write error poisons/closes the lane and rejects queued/future frames. A shared RAII startup transaction owns child, pipes, stop state, and partial workers until commit, and injected every-stage failures prove kill, reap, join, and retry. |
| `/root/merge_wave1/wave1_round11_cross_review` (Planck) | No high-conviction blocker; history, source-bound evidence, six Round 10 fixes, cross-stack ordering, and file-size thresholds were clean. | CLEAN / APPROVE at the reviewed head. The independent Rust/TS blockers above remained hard gates and were fixed before this ledger update. |

The user-capped twelfth and final full round reviewed frozen exact evidence head `59c97ec61baed861bb50b468f17f3af01b84b662`. Each reviewer read the complete exact thermo-nuclear skill and improvement plan through EOF before inspection; personal identities were not retained, so only exact task IDs are recorded:

| Reviewer | Final verdict | Non-blocking disposition |
| --- | --- | --- |
| `/root/merge_wave1/wave1_round12_ts_review` | CLEAN / APPROVE; no critical correctness, data-loss, or security blocker. TypeScript, all 85 files / 715 tests, production build, and range diff-check passed on the frozen head. | Defer the stale historical `tests/performance/optimization/p5-tauri-ssh.md` description of the subsequently implemented WebView window; authoritative current behavior and evidence are recorded here. Continue monitoring near-threshold production files and the honestly `NOT_CAPTURED` Stage 0/platform rows. |
| `/root/merge_wave1/wave1_round12_rust_review` | CLEAN / APPROVE; no critical correctness, data-loss, or security blocker across reveal ordering, writer poisoning, transactional startup, transport, input, replay, transfer, and shutdown ownership. | Near-threshold files, a defensive missing-stdio reap path whose production invariant always supplies piped handles, and pathological large-input subprocess shutdown remain low-risk follow-up. A live combined Rust rerun was environmentally unable to compile because of disk quota; the exact Round 11 full gate above remains source-bound and green. |
| `/root/merge_wave1/wave1_round12_cross_review` | CLEAN / APPROVE; no critical correctness, data-loss, or security blocker across history, conflicts, protocol/credit/input/transfer invariants, evidence, or cleanup. | Pre-existing giant files, later-scope platform/evidence rows, and speculative malformed native-to-JS hardening remain explicitly deferred. |

Under the capped final-round rule, these structural, speculative, documentation, and low-risk observations do not reopen Wave 1. No Wave 2 optimization was started.

Before the final flow redesign, three independent architecture reviewers read the complete exact skill and plan and reviewed the measured alternatives without editing source:

| Reviewer | Recommendation | Decision |
| --- | --- | --- |
| `/root/merge_wave1/wave1_input_arch_a` (Nietzsche) | One persistent `no-output,ignore-size,no-detach-on-destroy` command client per host protocol connection, with one cross-pane FIFO and exact fences; no timer or priority queue. | Adopted as the prerequisite input path. |
| `/root/merge_wave1/wave1_credit_arch_b` (Einstein) | Retain the single ordered event FIFO and add negotiated cumulative host/native/JS delivery credit so already-admitted bytes cannot strand responses. | Adopted with byte and raw-record credit because adjacent coalescing changes record shape. |
| `/root/merge_wave1/wave1_flow_arch_c` (Popper) | Prefer cumulative admission credit over a native pull mailbox; a native-only mailbox cannot cap host/socket debt. | Adopted. Native-only pull and all timer/pause/priority variants were rejected. |

Fresh post-fix reviewer identities and final verdicts are appended after their stable-tree review; any new finding remains a blocker until fixed and re-reviewed.
